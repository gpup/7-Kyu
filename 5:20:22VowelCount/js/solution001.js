//Ok 7Kyu are a little much but this shouldn't be too hard. Let's see what I was doing wrong.

function getCount(str){
    let vowelCount = 0;
    for(let i = 0; i < str.length; i++){
      if(i === "a","e","i","0","u"){
        return vowelCount;
      }
        }
  }