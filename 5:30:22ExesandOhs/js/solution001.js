//I was so close to getting this one. had all the parts just didn't put them together in the right order.

function XO(str){
    return str.toLowerCase().split('x').length === str.toLowerCase().split('o').length;
  }