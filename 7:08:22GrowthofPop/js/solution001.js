//Just couldn't get this to work. Need more work on iterating and plugging in objects

function nbYear(p0, percent, aug, p) {
    let model = p0 + aug + (p0 * (percent*.01));
    for(let i = 0; i >= p; i++){
      
    }
}